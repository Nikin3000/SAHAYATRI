<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proto3">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo2"
        android:label="SahaYatri"
        android:roundIcon="@mipmap/logo2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".CustomerActivity.CustomerSignupActivity"
            android:exported="false" />
        <activity
            android:name=".CustomerActivity.CustomerLoginActivity"
            android:exported="false" />
        <activity android:name=".DriverActivity.DriverSignupActivity" />
        <activity android:name=".DriverActivity.RiderLoginActivity" />
        <activity android:name=".CustomerActivity.CusAccActivity" />
        <activity android:name=".CustomerActivity.CusMenuActivity" />
        <activity android:name=".CustomerActivity.CustomersMapActivity" />
        <activity android:name=".CustomerActivity.CusSetProfileActivity" />
        <activity android:name=".DriverActivity.DriverSetProfileActivity" />
        <activity android:name=".DriverActivity.DrivAccActivity" />
        <activity android:name=".DriverActivity.DrivMenuActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".DriverActivity.DriversMapActivity"
            android:label="@string/title_activity_drivers_map" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".GetStarted" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>